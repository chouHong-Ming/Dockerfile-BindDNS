version: "3.7"
services:
  server:
    build: ..
    ports:
      - "53:53/udp"
    volumes:
      - ./named.conf:/etc/named.conf
      - ./var_named:/var/named
    environment:
      - NAMEDCONF=/etc/named.conf
      - LOG_LEVEL=5


